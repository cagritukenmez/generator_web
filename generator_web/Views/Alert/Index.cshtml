@* @model generator_web.Models.IndexViewModel

<div class="sistem-alarm">
    <div class="sistem-info">
        <h2>🚨 Sistem Alarm Durumu</h2>
    </div>

    @if (Model.Alerts != null && Model.Alerts.Count > 0)
    {
        @foreach (var alert in Model.Alerts)
        {
            <div class="alert alert-@alert.Type">
                <span class="alert-icon">⚠️</span>
                <span class="alert-message">@alert.Message</span>
                <span class="alert-time">@alert.CreatedAt.ToString(" HH:mm")</span>
            </div>
        }
    }
    else
    {
        <p>✅ Sistem normal çalışıyor</p>
    }
</div> *@